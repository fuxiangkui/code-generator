package ${packageName}.dao.${model}.impl;

import ${packageName}.dao.${model}.${javaDomainName}ESDao;
import ${packageName}.model.${model}.${javaDomainName}Entity;
import com.mobanker.framework.es.BaseDaoImpl;
import com.mobanker.framework.tracking.EETransaction;
import org.springframework.stereotype.Repository;

import java.util.Arrays;
import java.util.List;

/**
* Created by CodeGeneration on ${dataTime}.
* ${domainDesc}
*/
@Repository
public class ${javaDomainName}ESDaoImpl extends BaseDaoImpl<${javaDomainName}Entity> implements ${javaDomainName}ESDao {

    @EETransaction(type = "SQL", name = "${javaDomainName}ESDaoImpl.save")
    @Override
    public void save(${javaDomainName}Entity entity) throws Exception {
        //this.insert(entity);
        this.addOrUpdate(Arrays.asList(entity));
    }

    @EETransaction(type = "SQL", name = "${javaDomainName}ESDaoImpl.saveList")
    @Override
    public void saveList(List<${javaDomainName}Entity> entities) throws Exception {
        this.addOrUpdate(entities);
    }

}
